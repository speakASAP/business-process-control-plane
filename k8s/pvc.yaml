apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: business-process-control-plane-data
  namespace: statex-apps
  labels:
    app: business-process-control-plane
    managed-by: codex
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
